#include <stdio.h>

/**
  *main - Entry point
  *Description - 'ptints alphabets in lower and uppercase'
  *Return: always 0 (Success)
  */

int main(void)
{
	int a = 97;
	int b = 65;

	while (if a <= 122)
	{
		putchar(a);
		a++;
	}
	while (b <= 90)
	{
		putchar(b);
		b++;
	}
	putchar("\n");
	return (0);
}
